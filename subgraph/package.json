{
  "name": "streamer-subgraph",
  "license": "GPL-3.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goerli": "graph deploy --studio --network goerli streamer",
    "deploy:mainnet": "graph deploy --studio --network mainnet streamer",
    "local-node": "docker-compose up",
    "create-local": "graph create --node http://localhost:8020/ streamer",
    "remove-local": "graph remove --node http://localhost:8020/ streamer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 streamer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
